# Orchestrator config (copy to .env for local dev)
PORT=8787

# Base URL of a vox-pop-core deployment (the audio call/response engine).
VOXPOP_BASE_URL=http://localhost:8080

# Where the player PWA is served (used to build prompt deep links).
APP_BASE_URL=http://localhost:5173
