{
    "lexicon": 1,
    "id": "game.bardcast.character.profile",
    "defs": {
        "main": {
            "type": "record",
            "description": "A Bardcast character's durable identity: who they are, independent of any campaign. Lives in the player's own repo, keyed on their DID, so it stays portable off Bardcast. Campaign-specific mechanics live in game.bardcast.character.sheet inside the campaign's space, never here. One record per character at the well-known `self` rkey.",
            "key": "literal:self",
            "record": {
                "type": "object",
                "required": ["displayName", "createdAt"],
                "properties": {
                    "player": {
                        "type": "string",
                        "format": "did",
                        "description": "DID of the player who portrays this character. The durable identity key the character hangs off of."
                    },
                    "displayName": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 120,
                        "description": "The character's name as it appears in narration."
                    },
                    "concept": {
                        "type": "string",
                        "maxLength": 300,
                        "description": "One-line concept, e.g. 'a guilt-ridden paladin who left her order'."
                    },
                    "pronouns": {
                        "type": "string",
                        "maxLength": 40
                    },
                    "drives": {
                        "type": "array",
                        "maxLength": 32,
                        "items": { "type": "string", "maxLength": 200 },
                        "description": "What motivates the character, across every campaign they appear in. Used by the narrative engine."
                    },
                    "createdAt": {
                        "type": "string",
                        "format": "datetime",
                        "description": "Real ISO 8601 creation time. NOT the in-world date (see stateEvent.storyDate)."
                    }
                }
            }
        }
    }
}
